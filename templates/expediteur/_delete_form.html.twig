<form method="post" action="{{ path('expediteur_delete', {'id': expediteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ expediteur.id) }}">
    <button class="btn">Delete</button>
</form>
